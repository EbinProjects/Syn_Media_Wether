
1.The API key is located in the Utils package inside the Constants class. Before building the APK, place the API key there.

2.The date and time picker library used is "Localdate", which requires API level 26 or above to run the application.

3.The APK build is located inside the path: SynmediaWether\app\build\outputs\apk.

4.Access location using the com.google.android.gms library.And its background fetching time interval is 5000 milliseconds.